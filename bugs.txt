Внешний вид:
- под разные размеры экранов отдельные части масштабируются некорректно
- в файлах результатов у русских наименований проблема с раскладкой. В интерфейсе все корректно
- изменить ширину колонок в таблицах
- вариант интерфейса с русским языком

Функционал:
- нужно доделать функции удаления и копирования файлов с древом
- создание кнопки all select для выбора строк не мышью
- в варианте ClickHouse наладить движок MergeTree

Прочее:
- оптимизация кода -> попробовать библиотеки готовые для compare files
- реализовать обновление таблицы summary df после копирования файлов
